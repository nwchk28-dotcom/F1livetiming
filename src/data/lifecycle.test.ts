import {describe,expect,it} from 'vitest'
import type {RaceWeekend,SessionState} from '../types'
import {applyOfficialRaceResults,applyRaceGrid,attachGrid,competitionFromSession,isQualifyingComplete,selectNextEvent,sessionTimeRemaining,shouldProjectChampionship} from './lifecycle'
import {emptySession} from './sources'

const event=(round:number,date:string):RaceWeekend=>({season:2026,round,meetingName:`Race ${round}`,circuit:'Circuit',locality:'City',country:'Japan',qualifyingStart:date,raceStart:date,timeZone:'Asia/Tokyo'})

describe('session lifecycle',()=>{
  it('shows started qualifying',()=>{const s={...emptySession(),sessionName:'Qualifying',status:'STARTED'} as SessionState;expect(competitionFromSession(s)).toBe('QUALIFYING')})
  it('keeps an aborted race visible during a red flag',()=>{const s={...emptySession(),sessionName:'Race',status:'ABORTED'} as SessionState;expect(competitionFromSession(s)).toBe('RACE')})
  it('keeps final race and qualifying views available for 48 hours',()=>{const s={...emptySession(),sessionName:'Race',status:'FINISHED',sessionEnd:'2026-09-06T15:00:00Z'} as SessionState;expect(competitionFromSession(s,undefined,new Date('2026-09-08T14:59:59Z'))).toBe('RACE');expect(competitionFromSession(s,undefined,new Date('2026-09-08T15:00:00Z'))).toBe('IDLE')})
  it('falls back to the scheduled race window when feed end is absent',()=>{const s={...emptySession(),sessionName:'Race',status:'FINISHED'} as SessionState,e={...event(13,'2026-09-05T14:00:00Z'),raceStart:'2026-09-06T13:00:00Z'};expect(competitionFromSession(s,e,new Date('2026-09-08T15:59:59Z'))).toBe('RACE');expect(competitionFromSession(s,e,new Date('2026-09-08T16:00:00Z'))).toBe('IDLE')})
  it('projects championship only while the race is active',()=>{expect(shouldProjectChampionship('RACE','STARTED')).toBe(true);expect(shouldProjectChampionship('RACE','ABORTED')).toBe(true);expect(shouldProjectChampionship('RACE','FINISHED')).toBe(false)})
  it('keeps qualifying visible between Q1, Q2 and Q3',()=>{const s={...emptySession(),sessionName:'Qualifying',status:'FINISHED'} as SessionState,e={...event(13,'2026-09-05T14:00:00Z'),raceStart:'2026-09-06T13:00:00Z'};expect(competitionFromSession(s,e,new Date('2026-09-05T14:25:00Z'))).toBe('QUALIFYING');expect(competitionFromSession(s,e,new Date('2026-09-05T18:00:00Z'))).toBe('IDLE')})
  it('uses live session times before the season schedule has loaded',()=>{const s={...emptySession(),sessionName:'Qualifying',status:'FINISHED',sessionStart:'2026-09-05T14:00:00Z',sessionEnd:'2026-09-05T15:00:00Z'} as SessionState;expect(competitionFromSession(s,undefined,new Date('2026-09-05T15:00:01Z'))).toBe('QUALIFYING')})
  it('does not revive a finished qualifying session without its event window',()=>{const s={...emptySession(),sessionName:'Qualifying',status:'FINISHED'} as SessionState;expect(competitionFromSession(s)).toBe('IDLE')})
  it('excludes sprint sessions',()=>expect(competitionFromSession({...emptySession(),sessionName:'Sprint Qualifying',status:'STARTED'})).toBe('IDLE'))
  it('selects next qualifying',()=>expect(selectNextEvent([event(1,'2026-01-01T00:00:00Z'),event(2,'2026-03-01T00:00:00Z')],new Date('2026-02-01'))?.round).toBe(2))
  it('ticks an extrapolating session clock every second',()=>{const s={...emptySession(),timeRemaining:'00:10:00',clockUpdatedAt:'2026-09-05T14:00:00Z',clockRunning:true};expect(sessionTimeRemaining(s,Date.parse('2026-09-05T14:00:07Z'))).toBe('00:09:53')})
  it('freezes a non-extrapolating session clock',()=>{const s={...emptySession(),timeRemaining:'00:02:30',clockUpdatedAt:'2026-09-05T14:00:00Z',clockRunning:false};expect(sessionTimeRemaining(s,Date.parse('2026-09-05T14:01:00Z'))).toBe('00:02:30')})
  it('distinguishes a Q break from completed Q3',()=>{const s={...emptySession(),sessionName:'Qualifying',status:'FINISHED',sessionEnd:'2026-09-05T15:00:00Z'} as SessionState;expect(isQualifyingComplete(s,Date.parse('2026-09-05T14:30:00Z'))).toBe(false);expect(isQualifyingComplete(s,Date.parse('2026-09-05T15:00:00Z'))).toBe(true)})
  it('applies the final race grid without replacing live positions',()=>{const s={...emptySession(),drivers:[{position:3,previousPosition:4,gridPosition:99,number:'1',code:'AAA',fullName:'A',team:'X',teamColor:'#f00',bestLap:'1:00',lastLap:'1:00',gap:'',interval:'',sectors:[],tyre:{compound:'SOFT' as const,laps:1},pitStops:0,status:'RUNNING' as const}]};const result=applyRaceGrid(s,{'1':7});expect(result.drivers[0]).toMatchObject({position:3,gridPosition:7})})
  it('applies official post-race classification and finished statuses',()=>{const s={...emptySession(),status:'FINISHED' as const,drivers:[{position:2,previousPosition:2,gridPosition:19,number:'12',code:'ANT',fullName:'Antonelli',team:'Mercedes',teamColor:'#0ff',bestLap:'1:23',lastLap:'1:24',gap:'',interval:'',sectors:[],tyre:{compound:'MEDIUM' as const,laps:17},pitStops:2,status:'RUNNING' as const}]},result=applyOfficialRaceResults(s,[{number:'12',position:1,gridPosition:19,status:'FINISHED',gap:'1:51:15.281',interval:'—',points:25}]);expect(result.drivers[0]).toMatchObject({position:1,gridPosition:19,status:'FINISHED',points:25,tyre:{compound:'MEDIUM',laps:17}})})
  it('attaches grid changes',()=>{const q={...emptySession(),drivers:[{position:1,previousPosition:1,gridPosition:1,number:'1',code:'AAA',fullName:'A',team:'X',teamColor:'#f00',bestLap:'1:00',lastLap:'1:00',gap:'',interval:'',sectors:[],tyre:{compound:'SOFT' as const,laps:1},pitStops:0,status:'FINISHED' as const}]};expect(attachGrid(q,{'1':4}).drivers[0].gridChange).toBe(3)})
})
